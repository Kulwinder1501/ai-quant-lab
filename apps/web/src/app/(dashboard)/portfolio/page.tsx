/**
 * `/portfolio` is the sidebar's name for the positions-and-orders view.
 *
 * This file was a byte-for-byte copy of `positions-orders/page.tsx`, down to the exported
 * function still being called `PositionsOrdersPage` -- two routes that had to be edited in
 * lockstep with nothing saying so. Re-exported instead, so the tab-parsing logic has one home.
 */
export { default } from "../positions-orders/page";
